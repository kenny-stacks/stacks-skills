---
phase: 07-stack-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true

must_haves:
  truths:
    - "Developer can find Node.js version requirements in README"
    - "Developer can find Rendezvous installation command in README"
    - "Developer can find verification command (npx rv --version) in README"
  artifacts:
    - path: "README.md"
      provides: "Rendezvous installation instructions with Node.js requirements"
      contains: "@stacks/rendezvous"
  key_links:
    - from: "README.md"
      to: "developer installation"
      via: "documented commands"
      pattern: "npm install -D @stacks/rendezvous"
---

<objective>
Document Rendezvous installation requirements and instructions in README.md to enable fuzz testing capability.

Purpose: Establish the installation foundation for property-based fuzz testing with Rendezvous. Developers need clear instructions for adding the fuzzer to their Clarinet projects.

Output: Updated README.md with Node.js requirements section and Rendezvous installation instructions.
</objective>

<execution_context>
@/Users/kenny/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kenny/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-stack-integration/07-RESEARCH.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README with Rendezvous installation instructions</name>
  <files>README.md</files>
  <action>
Update the Requirements section in README.md to include:

1. **Node.js version requirements:**
   - Add explicit version requirement: "Node.js 20, 22, or 24 (LTS versions)"
   - Add verification command: `node --version`
   - Add npm version requirement: "npm 8.3.0 or higher"

2. **Rendezvous installation section:**
   Add a new "Fuzz Testing (Optional)" section after Requirements with:
   - Brief explanation: "For property-based fuzz testing with Rendezvous"
   - Installation command: `npm install -D @stacks/rendezvous@0.13.1`
   - Verification command: `npx rv --version` (expected output: 0.13.1)
   - Note: "Requires Node.js 20+ (LTS versions recommended)"

3. **Keep existing structure intact:**
   - Do NOT remove existing Clarinet requirement
   - Do NOT modify the skill description or workflow sections
   - Place Fuzz Testing section logically (after Requirements, before License)

**Format guidance from research:**
- Use exact version 0.13.1 (not range like ^0.13.0) for reproducibility
- Install as devDependency (-D flag)
- Use npx for execution (not global install)
  </action>
  <verify>
1. Check README.md contains "Node.js 20, 22, or 24"
2. Check README.md contains "@stacks/rendezvous"
3. Check README.md contains "npx rv --version"
4. Verify markdown renders correctly (no syntax errors)
  </verify>
  <done>
README.md contains:
- Node.js version requirements (20/22/24 LTS)
- Rendezvous installation command with exact version
- Verification command for npx rv
- All existing content preserved
  </done>
</task>

</tasks>

<verification>
After task completion, verify Phase 7 success criteria:

1. **FUZZ-01 (partial):** Installation foundation established
   - Verify: README documents `npm install -D @stacks/rendezvous@0.13.1`

2. **Success Criteria from Roadmap:**
   - Developer can find installation command: grep "@stacks/rendezvous" README.md
   - Version verification documented: grep "npx rv --version" README.md
   - Node.js compatibility documented: grep "Node.js 20" README.md
</verification>

<success_criteria>
- [ ] README.md Requirements section includes Node.js 20/22/24 LTS requirement
- [ ] README.md has new Fuzz Testing section with installation command
- [ ] Installation command uses exact version: @stacks/rendezvous@0.13.1
- [ ] Verification command documented: npx rv --version
- [ ] No existing README content removed or broken
</success_criteria>

<output>
After completion, create `.planning/phases/07-stack-integration/07-01-SUMMARY.md`
</output>
